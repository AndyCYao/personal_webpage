<VirtualHost *:80>
    # WSGIScriptAlias /recommender /var/www/FlaskApps/FlaskApps.wsgi
    # <Directory /var/www/FlaskApps/Recommender/>
    #    Order allow,deny
    #    Allow from all
    #</Directory>

    ServerName www.andy-yao.com
    ServerAlias andy-yao.com
    ServerAdmin andy@andy-yao.com
    DocumentRoot /var/www/andy-yao.com/build

    ErrorLog ${APACHE_LOG_DIR}/error.log
    LogLevel warn
    CustomLog ${APACHE_LOG_DIR}/access.log combined
# RewriteEngine on
# RewriteCond %{SERVER_NAME} =www.andy-yao.com [OR]
# RewriteCond %{SERVER_NAME} =andy-yao.com
# RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>